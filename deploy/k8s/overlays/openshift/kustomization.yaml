---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: eda-playground-openshift

namespace: eda-playground

resources:
  - ../../base
  - route.yaml

labels:
  - pairs:
      app.kubernetes.io/instance: eda-playground
      app.kubernetes.io/version: "1.0.0"
