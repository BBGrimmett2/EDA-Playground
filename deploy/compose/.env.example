# Docker Compose Environment Variables

# Port to expose on the host
# Container always uses 8080 internally, this maps to host
HOST_PORT=8080

# Uncomment to use a specific image tag
# IMAGE_TAG=latest

# Uncomment to override Node environment
# NODE_ENV=production
