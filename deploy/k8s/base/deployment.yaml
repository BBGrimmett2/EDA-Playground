---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: eda-playground
  labels:
    app: eda-playground
    app.kubernetes.io/name: eda-playground
    app.kubernetes.io/component: web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eda-playground
  template:
    metadata:
      labels:
        app: eda-playground
        app.kubernetes.io/name: eda-playground
        app.kubernetes.io/component: web
    spec:
      serviceAccountName: default
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: eda-playground
          image: ghcr.io/bbgrimmett2/eda-playground:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "8080"
            - name: ALLOW_SELF_SIGNED_CERTS
              value: "false"
            # AAP OAuth Configuration (from secret)
            - name: AAP_BASE_URL
              valueFrom:
                secretKeyRef:
                  name: aap-oauth-secret
                  key: AAP_BASE_URL
                  optional: true
            - name: AAP_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: aap-oauth-secret
                  key: AAP_CLIENT_ID
                  optional: true
            - name: AAP_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: aap-oauth-secret
                  key: AAP_CLIENT_SECRET
                  optional: true
            - name: AAP_REDIRECT_URI
              valueFrom:
                secretKeyRef:
                  name: aap-oauth-secret
                  key: AAP_REDIRECT_URI
                  optional: true
            - name: COOKIE_DOMAIN
              valueFrom:
                secretKeyRef:
                  name: aap-oauth-secret
                  key: COOKIE_DOMAIN
                  optional: true
            - name: COOKIE_SECURE
              valueFrom:
                secretKeyRef:
                  name: aap-oauth-secret
                  key: COOKIE_SECURE
                  optional: true
            - name: SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: aap-oauth-secret
                  key: SESSION_SECRET
                  optional: true
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 0
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 30
          volumeMounts:
            - name: integrations
              mountPath: /app/integrations/custom
              readOnly: true
      volumes:
        - name: integrations
          configMap:
            name: eda-playground-integrations
            optional: true
