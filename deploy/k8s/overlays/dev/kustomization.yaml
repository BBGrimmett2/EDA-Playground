---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: eda-playground-dev

namespace: eda-playground

resources:
  - namespace.yaml
  - ../../base
  - route.yaml

patches:
  - path: patch-dev-env.yaml
    target:
      kind: Deployment
      name: eda-playground
